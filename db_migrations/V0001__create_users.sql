CREATE TABLE users (id VARCHAR(16) PRIMARY KEY, login VARCHAR(64) UNIQUE NOT NULL, name VARCHAR(128) NOT NULL, avatar TEXT, password_hash TEXT NOT NULL, online BOOLEAN DEFAULT FALSE, last_seen TIMESTAMPTZ DEFAULT NOW(), created_at TIMESTAMPTZ DEFAULT NOW())
